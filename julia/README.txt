some scripts for julia, see:
http://julialang.org/
for a program to execute them.

run script with:

julia script.jl

or:

include("path/to/script.jl")

inside julia.(replace "script.jl" with the script name)